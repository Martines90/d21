import { View, StyleSheet } from 'react-native'

const RowSpace = () => <View style={styles.RowSpace} />

const styles = StyleSheet.create({
  RowSpace: {
    marginVertical: 8,
  },
})

export default RowSpace
